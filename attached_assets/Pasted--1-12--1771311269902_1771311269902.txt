יש עדיין באג מספור מהותי במבנה, והוא לא נפתר.

הבעיה בפועל:
1) סדר בניינים לא יציב (לדוגמה מוצג 12, 11, 13 במקום סדר כרונולוגי ברור).
2) מספור דירות לא כרונולוגי:
   - בקומה 0 יש דירה "3"
   - בקומה 2 יש "201..206"
   - זה מנוגד לדרישה שלי למספור רציף.

דרישה עסקית מחייבת (Single Source of Truth):
A) מספור דירות חייב להיות רציף ועולה לפי סדר הקומות בפועל.
   דוגמה:
   - קומה 0: 1,2,3...
   - קומה 1: ממשיך מהרצף הקודם
   - קומה 2: ממשיך מהרצף הקודם
   אסור יותר לייצר פורמט 201/301 כברירת מחדל.

B) אם מוסיפים קומה תחתונה (למשל -1), כל הדירות הקיימות צריכות לעבור resequence אוטומטי
   כך שהרצף יישאר כרונולוגי מהקומה הנמוכה לגבוהה.

C) צריך לתמוך גם בתוויות טקסטואליות:
   - קומות: "קרקע", "טכנית", "גג"
   - יחידות: "חדר אופניים", "חדר עגלות" וכו'
   יחידות טקסטואליות לא שוברים את הרצף המספרי; הן נשמרות כתווית.

מה לבצע עכשיו:
1) להפריד בין:
   - display_label (מה שמוצג למשתמש)
   - seq/order (שדה מיון פנימי יציב למספור)
2) לבנות רה-סיקוונס אטומי (transactional ככל האפשר) לכל הבניין:
   - dry-run preview
   - apply
   - audit log של before/after
3) למיין יציב בכל הרמות:
   - בניינים לפי sort_index ואז natural numeric (11 לפני 12 לפני 13)
   - קומות לפי sort_index עולה
   - יחידות לפי sort_index עולה
4) להפעיל auto-resequence אחרי:
   - insert floor below
   - bulk add floors/units שמשנה סדר כרונולוגי
5) לעדכן UI:
   - תצוגה מסודרת לפי הרצף החדש
   - כפתור ברור: "תקן מספור" עם preview לפני אישור
   - לשנות ניסוחים ל: "הוסף קומות", "הוסף דירות"

קריטריוני קבלה (חובה):
1) עבור בניין עם קומות 0..7 ויחידות:
   - כל היחידות המספריות מופיעות כרצף מלא ללא קפיצות (1..N).
2) הוספת קומה -1 עם X יחידות:
   - היחידות ב-1- מקבלות את המספרים הראשונים,
   - וכל שאר הקומות מוזזות קדימה בהתאם.
3) יחידות טקסטואליות נשמרות כתווית ולא נדרסות במספור.
4) PM רואה ומנהל רק פרויקטים שיש לו membership אליהם (לא כל הפרויקטים).
5) לצרף הוכחה:
   - לפני/אחרי (screenshots),
   - פלט dry-run,
   - פלט apply,
   - רשימת בדיקות שעברו.

נא לבצע עכשיו את התיקון מקצה לקצה (DB + API + UI) ולהחזיר רק דוח תוצאות והוכחות.